apiVersion: apps/v1
kind: Deployment
metadata:
  name: ordersdep
  labels:
    app: orders-dep
spec:
  replicas: 5
  selector:
    matchLabels:
      app: orders-pod
  template:
    metadata:
      labels:
        app: orders-pod
    spec:
      containers:
      - name: orderscont
        image: yuvalshaul/orders:one
 